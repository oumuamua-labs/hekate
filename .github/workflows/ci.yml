name: CI workflow
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-D warnings -C target-feature=+aes"

jobs:
  changes:
    name: Detect changed crates
    runs-on: ubuntu-24.04-arm
    outputs:
      crypto: ${{ steps.f.outputs.crypto }}
      core: ${{ steps.f.outputs.core }}
      program: ${{ steps.f.outputs.program }}
      verifier: ${{ steps.f.outputs.verifier }}
      gadgets: ${{ steps.f.outputs.gadgets }}
      sdk: ${{ steps.f.outputs.sdk }}
      scribble: ${{ steps.f.outputs.scribble }}
      keccak: ${{ steps.f.outputs.keccak }}
      aes: ${{ steps.f.outputs.aes }}
      sha2: ${{ steps.f.outputs.sha2 }}
      rsa: ${{ steps.f.outputs.rsa }}
      pqc: ${{ steps.f.outputs.pqc }}
      hekate: ${{ steps.f.outputs.hekate }}
      workspace: ${{ steps.f.outputs.workspace }}
    steps:
      - uses: actions/checkout@v5
      - uses: dorny/paths-filter@v3
        id: f
        with:
          filters: |
            workspace:
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'deny.toml'
              - '.github/workflows/**'
            crypto:
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            core:
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            program:
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            verifier:
              - 'hekate-verifier/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            gadgets:
              - 'hekate-gadgets/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            sdk:
              - 'hekate-sdk/**'
              - 'hekate-verifier/**'
              - 'hekate-gadgets/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            scribble:
              - 'hekate-scribble/**'
              - 'hekate-sdk/**'
              - 'hekate-gadgets/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            keccak:
              - 'hekate-keccak/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            aes:
              - 'hekate-aes/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            sha2:
              - 'hekate-sha2/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            rsa:
              - 'hekate-rsa/**'
              - 'hekate-sha2/**'
              - 'hekate-gadgets/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            pqc:
              - 'hekate-pqc/**'
              - 'hekate-verifier/**'
              - 'hekate-sdk/**'
              - 'hekate-prover-sys/**'
              - 'hekate-scribble/**'
              - 'hekate-keccak/**'
              - 'hekate-gadgets/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
            hekate:
              - 'hekate/**'
              - 'hekate-pqc/**'
              - 'hekate-keccak/**'
              - 'hekate-aes/**'
              - 'hekate-sha2/**'
              - 'hekate-rsa/**'
              - 'hekate-scribble/**'
              - 'hekate-sdk/**'
              - 'hekate-verifier/**'
              - 'hekate-gadgets/**'
              - 'hekate-prover-sys/**'
              - 'hekate-program/**'
              - 'hekate-core/**'
              - 'hekate-crypto/**'
              - 'Cargo.toml'
              - 'Cargo.lock'

  fmt:
    name: Rustfmt
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all --check

      - name: PMULL enabled
        run: cargo rustc --release -p hekate-core -- --print cfg | grep -qx 'target_feature="aes"'

      - name: License headers
        run: |
          bad=$(git ls-files '*.rs' '*.fbs' ':!hekate-sdk/src/generated/*_generated.rs' \
            | while read -r f; do
                head -10 "$f" | grep -q 'SPDX-License-Identifier: AGPL-3.0-only' \
                  && head -10 "$f" | grep -q 'SPDX-FileCopyrightText' || echo "$f"
              done)
          [ -z "$bad" ] || { echo "missing SPDX header:"; echo "$bad"; exit 1; }

      - name: License files present
        run: |
          bad=$(git ls-files '*/Cargo.toml' | xargs -n1 dirname \
            | while read -r d; do
                for n in LICENSE NOTICE LICENSE-EXCEPTION; do
                  [ -f "$d/$n" ] || echo "$d/$n"
                done
              done)
          [ -z "$bad" ] || { echo "missing license file:"; echo "$bad"; exit 1; }

  audit:
    name: Audit + deny
    needs: changes
    if: needs.changes.outputs.workspace == 'true'
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v5
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit, cargo-deny
      - run: cargo audit
      - run: cargo deny check

  test:
    name: ${{ matrix.crate }}
    needs: changes
    runs-on: ubuntu-24.04-arm
    strategy:
      fail-fast: false
      matrix:
        include:
          - crate: hekate-crypto
            filter: crypto
            test_args: "--all-features"
          - crate: hekate-core
            filter: core
            test_args: "--all-features"
          - crate: hekate-program
            filter: program
            test_args: "--all-features"
          - crate: hekate-verifier
            filter: verifier
            test_args: "--all-features"
          - crate: hekate-gadgets
            filter: gadgets
            test_args: "--release"
          - crate: hekate-sdk
            filter: sdk
            test_args: "--all-features"
          - crate: hekate-scribble
            filter: scribble
            test_args: "--all-features"
          - crate: hekate-keccak
            filter: keccak
            test_args: "--release"
          - crate: hekate-aes
            filter: aes
            test_args: "--release"
          - crate: hekate-sha2
            filter: sha2
            test_args: "--release"
          - crate: hekate-rsa
            filter: rsa
            test_args: "--release"
          - crate: hekate-pqc
            filter: pqc
            test_args: "--release"
          - crate: hekate
            filter: hekate
            test_args: "--release"
            clippy_args: "--features sha2,sha3,secure-memory,table-math,dhat-heap"
    steps:
      - name: Should run
        id: run
        env:
          CHANGES: ${{ toJSON(needs.changes.outputs) }}
          FILTER: ${{ matrix.filter }}
        run: |
          val=$(echo "$CHANGES" | jq -r --arg k "$FILTER" '.[$k] // "false"')
          if [ "$val" = "true" ]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
          else
            echo "skip=true" >> "$GITHUB_OUTPUT"
          fi

      - if: steps.run.outputs.skip == 'false'
        uses: actions/checkout@v5

      - if: steps.run.outputs.skip == 'false'
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - if: steps.run.outputs.skip == 'false'
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.crate }}

      - if: steps.run.outputs.skip == 'false'
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest

      - if: steps.run.outputs.skip == 'false'
        name: Clippy
        run: cargo clippy -p ${{ matrix.crate }} --all-targets ${{ matrix.clippy_args || '--all-features' }} -- -D warnings

      - if: steps.run.outputs.skip == 'false'
        name: Nextest
        run: cargo nextest run -p ${{ matrix.crate }} ${{ matrix.test_args }}
